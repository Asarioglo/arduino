$(".tempGauge-demo").tempGauge({
  // border color
  borderColor: "black",
  // border width
  borderWidth: 4,
  // default temperature
  defaultTemp: 0,
  // fill color
  fillColor: "red",
  // show label
  showLabel:true,
  // label size in pixels
  labelSize: 12,
  // maximum temperature
  maxTemp: 40,
  // minimum temperature
  minTemp: -10,
  // Temperature width
  width: 100
});
